nginx:
  build: ./nginx/
  links:
    - php
  ports:
    - PORT:80
  volumes_from:
    - app

php:
  build: ./php/
  expose:
    - 9000
  volumes_from:
    - app

app:
  command: "true"
  image: php:7.0-fpm
  volumes:
    - PATH:/var/www/html